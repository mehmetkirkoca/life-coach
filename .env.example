# Application Port Settings
# HOST_PORT: Port exposed on the host machine (browser access e.g., http://localhost:3030)
HOST_PORT=3030

# PORT: Port used inside the application container / server process
PORT=3000

# MongoDB Port Settings
# MONGO_HOST_PORT: Port exposed on the host machine for MongoDB
MONGO_HOST_PORT=27019
